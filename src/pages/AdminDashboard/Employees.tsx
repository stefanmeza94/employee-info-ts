import { useState } from 'react';
import AddContainer from '../../components/AddContainer';

const Employees = () => {
  return (
    <>
      <AddContainer />
    </>
  );
};

export default Employees;
